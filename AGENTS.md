# AGENTS.md — 项目编码与提交规范

本文件是参与本仓库开发的 Agent（或任何开发者）必须遵守的规范。所有在本仓库中编写的代码和提交都必须遵循以下规则。

## 1. 代码编写规范

### 1.1 实体类必须配中文属性注释

编写实体类（Entity / Model / Schema / DTO 等数据类）时，**类中的每一个属性都必须带有中文注释**，说明该属性代表什么含义。

要求：

- 每个属性上方或同行必须有中文注释，解释该属性的业务含义；
- 注释要说明「是什么」以及必要的「为什么」（如特殊约束、取值含义）；
- 示例：

```python
class User:
    """用户实体。"""

    id: int          # 用户唯一标识（主键，自增）
    username: str    # 登录用户名，全局唯一
    password_hash: str  # 密码加盐哈希值，禁止存储明文密码
    is_active: bool  # 账号是否可用；False 表示已禁用，登录时拒绝
    created_at: datetime  # 用户创建时间（UTC）
```

> 该规则适用于新增实体类以及修改已有实体类的属性时。

### 1.2 测试方法必须配中文注释

编写测试用例时，**每个测试方法之前必须有中文注释**，说明该测试方法的作用。

要求：

- 注释至少说明以下之一（或全部）：
  - 该测试**保护的行为**（验证的核心功能点）；
  - 该测试**覆盖的边界**（边界值、异常分支、极端输入）；
- 示例：

```python
class TestUserService:
    def test_create_user(self):
        # 保护行为：合法的用户名和密码应能成功创建用户，
        # 且密码以哈希形式存储、返回的用户对象包含正确的 id。
        ...

    def test_create_user_duplicate_username(self):
        # 边界情况：用户名已存在时，应抛出 DuplicateUsernameError，
        # 且数据库中不会产生第二条记录。
        ...
```

### 1.3 所有代码注释必须使用中文

**仓库内所有代码文件中的注释必须使用中文撰写，禁止使用英文注释（保留专有名词、标识符名、代码关键字除外）。**

要求：

- 行内注释（`#`、`//`、`/* */`、`<!-- -->` 等）、行尾注释、文档字符串（docstring）以及函数/类/方法的说明性注释，一律使用中文；
- 注释中的代码标识符（变量名、函数名、类名、参数名）可按原样保留，但说明文字必须是中文；
- 不新增英文注释；如遇历史遗留的英文注释，在改动该处代码时应一并翻译为中文；
- 禁止使用无意义的中文注释（如「这里是注释」「随便写写」等）。

## 2. 提交信息规范

**所有的代码提交，无论本地提交还是推送到远程，提交附带信息（commit message）必须使用中文。**

要求：

- commit message 使用中文撰写，简明描述本次改动的内容与目的；
- 建议采用「动词 + 对象」的句式，例如：
  - `新增用户注册接口及对应测试`
  - `修复订单状态更新时的并发问题`
  - `重构知识库检索模块，拆分召回与重排逻辑`
- 推送代码到远程仓库（`git push`）时，提交信息同样保持中文；
- 禁止使用英文、无意义的提交信息（如 `update`、`fix`、`aaa` 等）。

> 注：历史提交中可能存在英文信息，仅针对**本规范生效之后**产生的新提交生效；如需整理历史提交，需先与仓库维护者确认。

## 3. 提交时机规范

**每次修改代码之前，必须先检查仓库中是否存在未提交的内容；若存在，必须先将其提交，再进行修改。每次修改完代码之后，必须立即提交本次改动。**

要求：

- **修改前检查**：动手修改任何代码之前，先执行 `git status` 检查工作区与暂存区；若存在未提交的改动（包括新增、修改、删除、重命名文件），必须先完成一次提交，确保仓库处于干净状态后再开始修改；
- **修改后提交**：每次代码修改完成后（包括新增文件、修改文件、删除文件、重构、修复 bug 等），必须立即将本次改动提交到仓库，不允许跨多个改动累积后再提交；
- **提交信息**：本次提交的 commit message 必须遵循第 2 节「提交信息规范」（中文撰写）；
- **异常处理**：若某次修改尚未完成、暂时无法提交（例如正在进行中的半成品工作），应在提交信息中明确说明为临时/中间提交（如「WIP：……」，描述仍需使用中文），并在完成后再进行收尾提交；
- 提交前注意排除不应纳入版本控制的文件（如构建产物、依赖目录、本地配置文件等），遵守仓库已有的 `.gitignore` 约定。
