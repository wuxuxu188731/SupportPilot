# 项目架构总结(已过时/弃用)

> 项目路径: `D:\c66c6c6c6c666\javaBacked\python基础\day06`
> 技术栈: Python + FastAPI + SQLite + JWT + Alembic
> 架构模式: Protocol-based dependency injection + raw SQL (无 ORM)

---

## 目录

- [一、基类（数据库表对应的实体类）](#一基类数据库表对应的实体类)
- [二、自定义异常类](#二自定义异常类)
- [三、Service 类](#三service-类)
- [四、Store 实现类与 Seeder](#四store-实现类与-seeder)
- [五、路由工厂](#五路由工厂)
- [附录：数据库表完整字段一览](#附录数据库表完整字段一览)

---

## 一、基类（数据库表对应的实体类）

项目不使用 SQLAlchemy ORM，而是用 `@dataclass` 作为领域模型 + `Protocol` 作为存储接口 + 原生 `sqlite3` 实现持久化。Alembic 迁移脚本定义实际的 SQL 表结构。

### 共计: 10 个实体 dataclass（对应 10 张表）+ 1 张隐式表（无 Python 类）

---

### 1. `User` — 用户

| 项目 | 内容 |
|---|---|
| **文件** | `app/users/base.py` (行 4-9) |
| **表名** | `users` |
| **类型** | `@dataclass` |

| 字段 | 类型 | 含义 |
|---|---|---|
| `user_id` | `str` | 主键，创建时分配的 UUID |
| `username` | `str` | 唯一登录名，大小写不敏感（NOCASE 排序规则存储） |
| `password_hash` | `str` | bcrypt 哈希后的密码 |
| `created_at` | `str` | ISO 格式时间戳，默认 `CURRENT_TIMESTAMP` |

**作用**: 表示系统中的一个注册用户。

---

### 2. `Conversation` — 会话

| 项目 | 内容 |
|---|---|
| **文件** | `app/sessions/base.py` (行 4-9) |
| **表名** | `conversations` |
| **类型** | `@dataclass(frozen=True)` |

| 字段 | 类型 | 含义 |
|---|---|---|
| `conversation_id` | `str` | 主键，创建时分配的 UUID |
| `organization_id` | `str` | 外键 → `organizations.id`，租户/组织范围 |
| `user_id` | `str` | 外键 → `users.id`，会话所有者 |
| `system_prompt` | `str \| None` | 可选的系统提示词，设定 LLM 的行为 |

**作用**: 表示某个用户在某组织内的一个聊天会话。数据库中还有 `created_at`、`updated_at` 列但 dataclass 中未体现（由 SQL 自动维护）。

---

### 3. `Organization` — 组织

| 项目 | 内容 |
|---|---|
| **文件** | `app/organizations/base.py` (行 11-15) |
| **表名** | `organizations` |
| **类型** | `@dataclass(frozen=True)` |

| 字段 | 类型 | 含义 |
|---|---|---|
| `organization_id` | `str` | 主键，创建时分配的 UUID |
| `name` | `str` | 组织名称（2-100 字符） |
| `created_at` | `str` | ISO 格式时间戳，默认 `CURRENT_TIMESTAMP` |

**作用**: 表示一个多租户组织。所有会话和用户都在组织范围内，实现多租户隔离。

---

### 4. `Membership` — 组织成员关系

| 项目 | 内容 |
|---|---|
| **文件** | `app/organizations/base.py` (行 18-23) |
| **表名** | `memberships` |
| **类型** | `@dataclass(frozen=True)` |

| 字段 | 类型 | 含义 |
|---|---|---|
| `organization_id` | `str` | 复合主键之一，外键 → `organizations.id` |
| `user_id` | `str` | 复合主键之一，外键 → `users.id` |
| `role` | `MembershipRole` | 枚举：`"admin"` 或 `"agent"`，定义用户在组织内的权限 |
| `created_at` | `str` | ISO 格式时间戳 |

**作用**: 用户与组织的多对多关联表。一个用户可属于多个组织，一个组织可有多个用户。复合主键为 `(organization_id, user_id)`。

---

### 5. `OrganizationAccess` — 用户视角的组织信息（只读投影）

| 项目 | 内容 |
|---|---|
| **文件** | `app/organizations/base.py` (行 26-30) |
| **表名** | 非直接映射（通过 JOIN `memberships` 和 `organizations` 查询得来） |
| **类型** | `@dataclass(frozen=True)` |

| 字段 | 类型 | 含义 |
|---|---|---|
| `organization_id` | `str` | 组织 ID |
| `name` | `str` | 组织名称（来自 `organizations.name`） |
| `role` | `MembershipRole` | 用户在该组织中的角色 |

**作用**: 用于 `list_organizations` 的返回结果，表示用户对其所属组织的视图（含组织名和角色）。

---

### 6. `messages` 表（隐式表，无对应 Python 类）

| 项目 | 内容 |
|---|---|
| **定义位置** | `migrations/versions/0001_baseline.py` (行 59-87) |
| **表名** | `messages` |
| **存储方式** | 以原始 `dict` + JSON 序列化方式读写，无 dataclass 包装 |

| 字段 | 类型 | 含义 |
|---|---|---|
| `id` | `Integer` | 自增主键 |
| `conversation_id` | `Text` | 外键 → `conversations.id` |
| `seq` | `Integer` | 会话内的消息序号，唯一约束 `(conversation_id, seq)` |
| `role` | `Text` | 消息角色：`"user"`, `"assistant"`, `"tool"` 等 |
| `payload_json` | `Text` | 完整消息内容的 JSON 字符串 |
| `created_at` | `Text` | 默认 `CURRENT_TIMESTAMP` |

**作用**: 存储聊天消息历史。每条消息属于一个会话，携带序号和角色，以 JSON 格式存储完整消息体。

---

### 7. `Customer` — 客户

| 项目 | 内容 |
|---|---|
| **文件** | `app/customers/base.py` |
| **表名** | `customers` |
| **类型** | `@dataclass(frozen=True)` |

| 字段 | 类型 | 含义 |
|---|---|---|
| `customer_id` | `str` | 主键，创建时分配的 UUID（映射 DB 列 `id`） |
| `organization_id` | `str` | 外键 → `organizations.id`，租户范围 |
| `customer_no` | `str` | 企业内唯一的客户编号（如 `CUST-001`） |
| `name` | `str` | 客户姓名 |
| `email` | `str \| None` | 邮箱，可空 |
| `phone` | `str \| None` | 电话号码，可空 |
| `created_at` | `str` | ISO 格式时间戳 |

**作用**: 表示某个企业下的一个客户。同一 `customer_no` 可存在于不同企业，但同企业内唯一。

---

### 8. `Order` — 订单

| 项目 | 内容 |
|---|---|
| **文件** | `app/orders/base.py` |
| **表名** | `orders` |
| **类型** | `@dataclass(frozen=True)` |

| 字段 | 类型 | 含义 |
|---|---|---|
| `order_id` | `str` | 主键，创建时分配的 UUID（映射 DB 列 `id`） |
| `organization_id` | `str` | 外键 → `organizations.id`，租户范围 |
| `order_no` | `str` | 企业内唯一的订单号（如 `ORD-DELAY-001`） |
| `customer_id` | `str` | 外键 → `customers.id`，下单客户 |
| `status` | `OrderStatus` | 订单状态枚举（7 种状态） |
| `item_summary` | `str` | 商品摘要描述 |
| `total_amount_cents` | `int` | 总金额（整数分，禁止 float） |
| `currency` | `str` | 三位大写币种代码（如 `CNY`） |
| `placed_at` | `str` | 下单时间（ISO 8601） |
| `promised_ship_at` | `str \| None` | 承诺发货时间，可空 |
| `created_at` | `str` | ISO 格式时间戳 |
| `updated_at` | `str` | ISO 格式时间戳 |

**作用**: 表示某个企业客户下的订单。金额以整数分存储避免浮点误差。外键为复合约束 `(organization_id, customer_id)`，阻止跨企业客户关联。

---

### 9. `Shipment` — 物流

| 项目 | 内容 |
|---|---|
| **文件** | `app/shipments/base.py` |
| **表名** | `shipments` |
| **类型** | `@dataclass(frozen=True)` |

| 字段 | 类型 | 含义 |
|---|---|---|
| `shipment_id` | `str` | 主键，创建时分配的 UUID（映射 DB 列 `id`） |
| `organization_id` | `str` | 外键 → `organizations.id`，租户范围 |
| `shipment_no` | `str` | 企业内唯一的物流单号（如 `SHP-TRANSIT-001`） |
| `order_id` | `str` | 外键 → `orders.id`，关联的订单 |
| `carrier` | `str` | 承运商名称 |
| `tracking_no` | `str` | 快递跟踪号 |
| `status` | `ShipmentStatus` | 物流状态枚举（6 种状态） |
| `last_event` | `str \| None` | 最新物流事件描述，可空 |
| `shipped_at` | `str \| None` | 发货时间，可空 |
| `estimated_delivery_at` | `str \| None` | 预计送达时间，可空 |
| `delivered_at` | `str \| None` | 实际签收时间，可空 |
| `created_at` | `str` | ISO 格式时间戳 |
| `updated_at` | `str` | ISO 格式时间戳 |

**作用**: 表示物流记录。第一版：一个订单最多对应一条物流记录（通过 `UNIQUE(organization_id, order_id)` 保证）。

---

### 10. `Ticket` — 工单

| 项目 | 内容 |
|---|---|
| **文件** | `app/tickets/base.py` |
| **表名** | `tickets` |
| **类型** | `@dataclass(frozen=True)` |

| 字段 | 类型 | 含义 |
|---|---|---|
| `ticket_id` | `str` | 主键，创建时分配的 UUID（映射 DB 列 `id`） |
| `organization_id` | `str` | 外键 → `organizations.id`，租户范围 |
| `ticket_no` | `str` | 企业内唯一的工单号（如 `TKT-DELAY-001`） |
| `customer_id` | `str` | 外键 → `customers.id`，关联客户（必填） |
| `order_id` | `str \| None` | 外键 → `orders.id`，可选关联订单 |
| `created_by_user_id` | `str` | 外键 → `memberships.user_id`，创建人（必须同企业成员） |
| `assigned_to_user_id` | `str \| None` | 外键 → `memberships.user_id`，负责人（必须同企业成员），可空 |
| `summary` | `str` | 工单摘要/标题 |
| `category` | `TicketCategory` | 工单分类枚举（5 种） |
| `priority` | `TicketPriority` | 优先级枚举（4 级） |
| `status` | `TicketStatus` | 工单状态枚举（5 种） |
| `created_at` | `str` | ISO 格式时间戳 |
| `updated_at` | `str` | ISO 格式时间戳 |

**作用**: 表示客服工单。创建人和负责人必须通过 `memberships` 表的复合外键约束确保属于当前企业。可关联客户和可选订单，订单关联时通过三列复合外键确保订单属于同一客户和企业。

---

### 11. `TicketComment` — 工单评论

| 项目 | 内容 |
|---|---|
| **文件** | `app/tickets/base.py` |
| **表名** | `ticket_comments` |
| **类型** | `@dataclass(frozen=True)` |

| 字段 | 类型 | 含义 |
|---|---|---|
| `comment_id` | `str` | 主键，创建时分配的 UUID（映射 DB 列 `id`） |
| `organization_id` | `str` | 外键 → `organizations.id`，租户范围 |
| `ticket_id` | `str` | 外键 → `tickets.id`，所属工单 |
| `seq` | `int` | 工单内递增序号（≥1），唯一约束 `(org, ticket, seq)` |
| `author_user_id` | `str` | 外键 → `memberships.user_id`，评论作者（必须同企业成员） |
| `visibility` | `TicketCommentVisibility` | 可见性：`internal`（内部备注）或 `public`（公开回复） |
| `content` | `str` | 评论正文 |
| `created_at` | `str` | ISO 格式时间戳，由应用代码生成 |

**作用**: 表示工单的评论/回复。`seq` 通过 `SELECT MAX(seq) + 1` 在事务内自增，保证同一工单内评论严格有序。

---

### 辅助类型（非数据库表）

| 类型 | 文件 | 说明 |
|---|---|---|
| `MembershipRole` (enum) | `app/organizations/base.py:6-9` | 角色枚举：`ADMIN = "admin"`, `AGENT = "agent"` |
| `OrderStatus` (enum) | `app/orders/base.py` | 订单状态：`pending_payment`, `paid`, `processing`, `shipped`, `delivered`, `cancelled`, `refunded` |
| `ShipmentStatus` (enum) | `app/shipments/base.py` | 物流状态：`pending`, `picked_up`, `in_transit`, `delivered`, `exception`, `returned` |
| `TicketCategory` (enum) | `app/tickets/base.py` | 工单分类：`logistics`, `refund`, `damaged_item`, `product_question`, `other` |
| `TicketPriority` (enum) | `app/tickets/base.py` | 优先级：`low`, `medium`, `high`, `urgent` |
| `TicketStatus` (enum) | `app/tickets/base.py` | 工单状态：`open`, `in_progress`, `pending_customer`, `resolved`, `closed` |
| `TicketCommentVisibility` (enum) | `app/tickets/base.py` | 评论可见性：`internal`, `public` |
| `TenantContext` (dataclass) | `app/application/organization_service.py:18-22` | 运行时上下文：`user_id` + `organization_id` + `role` |
| `AccessTokenClaims` (dataclass) | `app/auth/tokens.py:9-11` | JWT 声明，仅含 `user_id` |
| `LoginResult` (dataclass) | `app/application/auth_service.py:19-23` | DTO：`access_token` + `token_type` + `expire_in` |
| `DemoSeedResult` (dataclass) | `app/demo_data/seeder.py` | Seeder 返回结果：`organization_id` + `customer_nos` + `order_nos` + `shipment_nos` + `ticket_nos`（均为 tuple） |
| `OrderDetails` (dataclass) | `app/application/customer_support_service.py:44-47` | 聚合结果：`order: Order` + `customer: Customer` |
| `LogisticsAvailability` (enum) | `app/application/customer_support_service.py:49-51` | 物流可用性：`NOT_CREATED = "not_created"`, `AVAILABLE = "available"` |
| `LogisticsDetails` (dataclass) | `app/application/customer_support_service.py:53-58` | 聚合结果：`order` + `customer` + `availability: LogisticsAvailability` + `shipment: Shipment \| None` |
| `TicketNumberFactory` (type alias) | `app/application/customer_support_service.py:42` | `Callable[[], str]`，工单号生成工厂类型 |

---

## 二、自定义异常类

### 共计: 37 个自定义异常类

---

### 用户相关 (2)

| # | 类名 | 文件 | 继承自 | 抛出场景 |
|---|---|---|---|---|
| 1 | `UserAlreadyExistError` | `app/users/base.py:12` | `ValueError` | `create_user` 时用户名已存在 |
| 2 | `UserNotFoundError` | `app/users/base.py:15` | `LookupError` | `get_by_username` / `get_by_id` 查无此人 |

---

### 会话相关 (1)

| # | 类名 | 文件 | 继承自 | 抛出场景 |
|---|---|---|---|---|
| 3 | `ConversationNotFoundError` | `app/sessions/base.py:12` | `LookupError` | `get_conversation` / `update_system_prompt` / `load_messages` / `append_messages` 中查不到指定会话 |

---

### 数据库迁移相关 (1)

| # | 类名 | 文件 | 继承自 | 抛出场景 |
|---|---|---|---|---|
| 4 | `DatabaseMigrationError` | `app/db/migrations.py:40` | `RuntimeError` | 旧版数据库 Schema 与预期不符，或检测到部分遗留表结构 |

---

### 组织/成员相关 (2)

| # | 类名 | 文件 | 继承自 | 抛出场景 |
|---|---|---|---|---|
| 5 | `MembershipNotFoundError` | `app/organizations/base.py:33` | `LookupError` | `get_membership` 查不到成员关系 |
| 6 | `MembershipAlreadyExistsError` | `app/organizations/base.py:37` | `ValueError` | `add_membership` 时成员关系已存在（UNIQUE 约束冲突） |

---

### 密码相关 (1)

| # | 类名 | 文件 | 继承自 | 抛出场景 |
|---|---|---|---|---|
| 7 | `PasswordPolicyError` | `app/auth/passwords.py:7` | `ValueError` | 密码 UTF-8 字节长度 < 8 或 > 72 |

---

### Token 相关 (1)

| # | 类名 | 文件 | 继承自 | 抛出场景 |
|---|---|---|---|---|
| 8 | `InvalidAccessTokenError` | `app/auth/tokens.py:13` | `ValueError` | JWT 解码失败、类型不是 `"access"`、或 `sub` 声明为空 |

---

### 认证服务相关 (3)

| # | 类名 | 文件 | 继承自 | 抛出场景 |
|---|---|---|---|---|
| 9 | `InvalidUsernameError` | `app/application/auth_service.py:25` | `ValueError` | 用户名不匹配正则 `^[a-z0-9_]{3,32}$` |
| 10 | `InvalidCredentialsError` | `app/application/auth_service.py:28` | `ValueError` | 登录时用户名或密码错误 |
| 11 | `InvalidAuthenticationError` | `app/application/auth_service.py:31` | `ValueError` | Token 无效、过期或用户已被删除 |

---

### 组织服务相关 (3)

| # | 类名 | 文件 | 继承自 | 抛出场景 |
|---|---|---|---|---|
| 12 | `InvalidOrganizationNameError` | `app/application/organization_service.py:25` | `ValueError` | 组织名称 < 2 或 > 100 字符 |
| 13 | `OrganizationAccessDeniedError` | `app/application/organization_service.py:29` | `LookupError` | 用户不是目标组织的成员 |
| 14 | `AdminRoleRequiredError` | `app/application/organization_service.py:33` | `PermissionError` | 非 admin 用户尝试添加成员 |

---

### 客户相关 (3)

| # | 类名 | 文件 | 继承自 | 抛出场景 |
|---|---|---|---|---|
| 15 | `CustomerNotFoundError` | `app/customers/base.py` | `LookupError` | `get_by_id` / `get_by_no` 查不到指定客户 |
| 16 | `CustomerAlreadyExistsError` | `app/customers/base.py` | `ValueError` | 同企业内 `customer_no` 重复创建 |
| 17 | `InvalidCustomerReferenceError` | `app/customers/base.py` | `ValueError` | `organization_id` 不存在或跨企业客户关联 |

---

### 订单相关 (3)

| # | 类名 | 文件 | 继承自 | 抛出场景 |
|---|---|---|---|---|
| 18 | `OrderNotFoundError` | `app/orders/base.py` | `LookupError` | `get_by_id` / `get_by_no` 查不到指定订单 |
| 19 | `OrderAlreadyExistsError` | `app/orders/base.py` | `ValueError` | 同企业内 `order_no` 重复创建 |
| 20 | `InvalidOrderReferenceError` | `app/orders/base.py` | `ValueError` | 跨企业客户关联、负金额或非法状态值 |

---

### 物流相关 (3)

| # | 类名 | 文件 | 继承自 | 抛出场景 |
|---|---|---|---|---|
| 21 | `ShipmentNotFoundError` | `app/shipments/base.py` | `LookupError` | `get_by_id` / `get_by_no` / `get_by_order_no` 查不到物流记录 |
| 22 | `ShipmentAlreadyExistsError` | `app/shipments/base.py` | `ValueError` | 同企业重复 `shipment_no`、`tracking_no` 或同一订单已存在物流记录 |
| 23 | `InvalidShipmentReferenceError` | `app/shipments/base.py` | `ValueError` | 跨企业订单关联或非法状态值 |

---

### 工单及评论相关 (4)

| # | 类名 | 文件 | 继承自 | 抛出场景 |
|---|---|---|---|---|
| 24 | `TicketNotFoundError` | `app/tickets/base.py` | `LookupError` | `get_by_id` / `get_by_no` / `list_comments` 时工单不存在 |
| 25 | `TicketAlreadyExistsError` | `app/tickets/base.py` | `ValueError` | 同企业内 `ticket_no` 重复创建 |
| 26 | `InvalidTicketReferenceError` | `app/tickets/base.py` | `ValueError` | 跨企业客户/订单/创建人/负责人关联、订单不属于指定客户、非成员的负责人 |
| 27 | `InvalidTicketCommentReferenceError` | `app/tickets/base.py` | `ValueError` | 评论作者非企业成员、空白内容或工单不存在 |

---

### 模拟数据 Seeder 相关 (2)

| # | 类名 | 文件 | 继承自 | 抛出场景 |
|---|---|---|---|---|
| 28 | `DemoSeedAdminRequiredError` | `app/demo_data/seeder.py` | `PermissionError` | 非 `admin` 角色的用户尝试执行 Seeder |
| 29 | `DemoDataConflictError` | `app/demo_data/seeder.py` | `ValueError` | Seeder 发现相同业务编号已被不兼容数据占用（如场景 A 的延迟订单意外有了物流记录） |

---

### 客服业务服务相关 (8) 🆕

| # | 类名 | 文件 | 继承自 | 抛出场景 |
|---|---|---|---|---|
| 30 | `InvalidSupportRequestError` | `app/application/customer_support_service.py:60` | `ValueError` | 业务参数校验失败：空白编号、摘要长度不在 5-500 范围、category/priority 类型错误、customer_no 或 order_no 均未提供 |
| 31 | `SupportOrderNotFoundError` | `app/application/customer_support_service.py:63` | `LookupError` | `get_order` / `get_logistics` 中订单不存在（屏蔽底层 `OrderNotFoundError`） |
| 32 | `SupportDataIntegrityError` | `app/application/customer_support_service.py:66` | `RuntimeError` | 订单引用的客户 ID 不存在（数据完整性问题，非正常业务路径） |
| 33 | `SupportCustomerNotFoundError` | `app/application/customer_support_service.py:69` | `LookupError` | `get_customer_by_no` / `create_ticket` 中客户编号不存在（屏蔽底层 `CustomerNotFoundError`） |
| 34 | `OrderCustomerMismatchError` | `app/application/customer_support_service.py:72` | `ValueError` | 同时提供 customer_no 和 order_no 时，订单归属客户与指定客户不一致 |
| 35 | `TicketNumberGenerationError` | `app/application/customer_support_service.py:75` | `RuntimeError` | 工单号生成工厂返回了空字符串或非字符串类型 / 连续 3 次生成都与已有工单号冲突 |
| 36 | `SupportOperationRejectedError` | `app/application/customer_support_service.py:78` | `ValueError` | 底层 Store 抛出 `InvalidTicketReferenceError` 或 `InvalidTicketCommentReferenceError` 时，Service 层统一转换为本异常 |
| 37 | `SupportTicketNotFoundError` | `app/application/customer_support_service.py:81` | `LookupError` | `add_ticket_note` 中工单不存在或属于其他企业（屏蔽底层 `TicketNotFoundError`） |

---

> **注意**: 所有 37 个异常类均为简单的 stub 类（`pass` 体），无自定义字段或方法，完全依赖父类构造函数传递消息字符串。

---

## 三、Service 类

### 共计: 4 个 Service 类

所有 Service 类均位于 `app/application/` 目录下，遵循"Protocol 接口 + 构造函数依赖注入"的架构模式。

---

### 1. `AuthService` — 认证服务

| 项目 | 内容 |
|---|---|
| **文件** | `app/application/auth_service.py` |
| **作用** | 处理用户注册、登录认证、Token 验证 |

#### 依赖

| 依赖 | 类型 | 来源 |
|---|---|---|
| `user_store` | `UserStore` (Protocol) | `app.users.base` |
| `token_service` | `AccessTokenService` | `app.auth.tokens` |
| `hash_password` | 函数 | `app.auth.passwords` |
| `verify_password` | 函数 | `app.auth.passwords` |

#### 方法列表 (4 个)

| # | 方法 | 签名 | 作用 | 返回值 |
|---|---|---|---|---|
| 1 | `_normalize_username` | `(username: str) -> str` | 去除空白、casefold 小写化，并校验 `^[a-z0-9_]{3,32}$` 格式，不合法则抛出 `InvalidUsernameError` | `str` |
| 2 | `registry` | `(*, username: str, password: str) -> User` | 规范化用户名 → 哈希密码 → 调用 `user_store.create_user()` 持久化 | `User` |
| 3 | `login` | `(*, username: str, password: str) -> LoginResult` | 查找用户 → 验证密码（用户名不存在时使用虚拟哈希防止时序攻击）→ 签发 JWT Token | `LoginResult` |
| 4 | `get_user_from_token` | `(*, token: str) -> User` | 解码 JWT → 提取 `user_id` → 查库返回用户，Token 无效或用户不存在时抛出 `InvalidAuthenticationError` | `User` |

---

### 2. `ChatService` — 聊天服务

| 项目 | 内容 |
|---|---|
| **文件** | `app/application/chat_service.py` |
| **作用** | 管理聊天会话的创建、系统提示词更新、执行一轮对话 |

#### 依赖

| 依赖 | 类型 | 来源 |
|---|---|---|
| `store` | `SessionStore` (Protocol) | `app.sessions.base` |
| `run_agent` | `Callable[..., LLMResponse]` | 由外部注入的 LLM 调用函数 |
| `locks` | `ConversationLockRegistry` | `app.concurrency.conversation_locks` |
| `TenantContext` | dataclass (类型标注) | `app.application.organization_service` |

#### 方法列表 (3 个)

| # | 方法 | 签名 | 作用 | 返回值 |
|---|---|---|---|---|
| 1 | `create_conversation` | `(*, context: TenantContext, system_prompt: str \| None = None) -> Conversation` | 从 `TenantContext` 中提取 `organization_id` 和 `user_id`，委托 `store.create_conversation()` 创建新会话 | `Conversation` |
| 2 | `update_system_prompt` | `(*, context: TenantContext, conversation_id: str, system_prompt: str) -> None` | 获取会话锁 → 委托 `store.update_system_prompt()` 更新系统提示词 | `None` |
| 3 | `chat` | `(*, context: TenantContext, conversation_id: str, question: str) -> LLMResponse` | 获取会话锁 → 加载会话和历史消息 → 构建 messages 列表（system prompt + 历史 + 新问题）→ 调用 `run_agent` → 持久化新消息 → 返回 LLM 响应 | `LLMResponse` |

---

### 3. `OrganizationService` — 组织/多租户服务

| 项目 | 内容 |
|---|---|
| **文件** | `app/application/organization_service.py` |
| **作用** | 管理多租户组织：创建组织、列出组织、解析租户上下文、添加成员 |

#### 依赖

| 依赖 | 类型 | 来源 |
|---|---|---|
| `organization_store` | `OrganizationStore` (Protocol) | `app.organizations.base` |
| `user_store` | `UserStore` (Protocol) | `app.users.base` |

#### 方法列表 (5 个)

| # | 方法 | 签名 | 作用 | 返回值 |
|---|---|---|---|---|
| 1 | `_normalize_name` | `(name: str) -> str` | 去除空白，校验长度 2-100，不合法则抛出 `InvalidOrganizationNameError` | `str` |
| 2 | `create_organization` | `(*, user_id: str, name: str) -> Organization` | 规范化名称 → 调用 `organization_store.create_with_admin()` 创建组织并设创建者为 admin | `Organization` |
| 3 | `list_organizations` | `(*, user_id: str) -> list[OrganizationAccess]` | 委托 `organization_store.list_for_user()` 列出用户所属的所有组织 | `list[OrganizationAccess]` |
| 4 | `get_tenant_context` | `(*, user_id: str, organization_id: str) -> TenantContext` | 查询成员关系 → 不存在则抛出 `OrganizationAccessDeniedError` → 返回 `TenantContext(user_id, organization_id, role)` | `TenantContext` |
| 5 | `add_member` | `(*, actor_user_id: str, organization_id: str, username: str, role: MembershipRole) -> Membership` | 验证操作者权限（必须是 admin）→ 查找目标用户 → 创建成员关系 | `Membership` |

---

---

### 4. `CustomerSupportService` — 确定性客服业务服务 🆕

| 项目 | 内容 |
|---|---|
| **文件** | `app/application/customer_support_service.py` |
| **作用** | 提供不依赖 LLM 的确定性客服业务用例：订单查询、物流查询、工单创建、内部备注 |

#### 依赖

| 依赖 | 类型 | 来源 |
|---|---|---|
| `customer_store` | `CustomerStore` (Protocol) | `app.customers.base` |
| `order_store` | `OrderStore` (Protocol) | `app.orders.base` |
| `shipment_store` | `ShipmentStore` (Protocol) | `app.shipments.base` |
| `ticket_store` | `TicketStore` (Protocol) | `app.tickets.base` |
| `ticket_no_factory` | `TicketNumberFactory` | 可选的工单号生成工厂，默认 `generate_ticket_no()` |

#### 公开方法 (4 个)

| # | 方法 | 签名 | 作用 | 返回值 |
|---|---|---|---|---|
| 1 | `get_order` | `(*, context: TenantContext, order_no: str) -> OrderDetails` | 查询订单及关联客户：trim 订单号 → 查订单 → 查客户 → 包装为 `OrderDetails`。订单不存在抛出 `SupportOrderNotFoundError`，客户引用断裂抛出 `SupportDataIntegrityError` | `OrderDetails` |
| 2 | `get_logistics` | `(*, context: TenantContext, order_no: str) -> LogisticsDetails` | 查询物流状态：先通过 `get_order` 确认订单存在 → 按订单号查物流 → 无物流记录返回 `NOT_CREATED`（正常结果，非异常）→ 有记录返回 `AVAILABLE` | `LogisticsDetails` |
| 3 | `create_ticket` | `(*, context: TenantContext, summary: str, category: TicketCategory, priority: TicketPriority, customer_no: str \| None = None, order_no: str \| None = None) -> Ticket` | 创建工单：校验摘要长度 → 校验枚举类型 → 从订单推导客户或使用显式客户 → 交叉校验客户与订单一致性 → 自动设置 `created_by_user_id`、`assigned_to_user_id`、`status=OPEN` → 工单号冲突时最多重试 3 次 | `Ticket` |
| 4 | `add_ticket_note` | `(*, context: TenantContext, ticket_no: str, content: str) -> TicketComment` | 添加内部备注：trim 并校验工单号和内容 → 内容不超过 2000 字符 → 查工单确认存在 → 以 `context.user_id` 为作者、`INTERNAL` 为可见性创建评论 | `TicketComment` |

#### 内部/辅助方法 (4 个)

| # | 方法 | 签名 | 作用 | 返回值 |
|---|---|---|---|---|
| 1 | `_required_text` | `(*, value: str, field_name: str) -> str` | 静态方法：trim 输入，为空则抛出 `InvalidSupportRequestError` | `str` |
| 2 | `_validate_summary` | `(*, summary: str) -> str` | 静态方法：trim 输入，长度不在 5-500 则抛出 `InvalidSupportRequestError` | `str` |
| 3 | `get_customer_by_no` | `(*, context: TenantContext, customer_no: str) -> Customer` | trim 客户号 → 查客户 Store → 不存在则抛出 `SupportCustomerNotFoundError` | `Customer` |
| 4 | `_new_ticket_no` | `() -> str` | 调用 `ticket_no_factory` 生成工单号 → 校验非空字符串 → trim 返回 | `str` |

#### 模块常量

| 常量 | 值 | 用途 |
|---|---|---|
| `MIN_TICKET_SUMMARY_LENGTH` | `5` | 工单摘要最小长度 |
| `MAX_TICKET_SUMMARY_LENGTH` | `500` | 工单摘要最大长度 |
| `MAX_TICKET_NUMBER_ATTEMPTS` | `3` | 工单号冲突时最大重试次数 |
| `MAX_TICKET_NOTE_LENGTH` | `2000` | 工单备注最大长度 |

### Service 依赖关系总览

```
AuthService ──────────> UserStore (Protocol)
                 └─────> AccessTokenService

ChatService ──────────> SessionStore (Protocol)
                └─────> Callable[..., LLMResponse]
                └─────> ConversationLockRegistry
                └─────> TenantContext (类型标注)

OrganizationService ──> OrganizationStore (Protocol)
                     └─> UserStore (Protocol)

CustomerSupportService ─> CustomerStore (Protocol)
                      └─> OrderStore (Protocol)
                      └─> ShipmentStore (Protocol)
                      └─> TicketStore (Protocol)
                      └─> TicketNumberFactory (Callable)
```

> 四个 Service 之间互不依赖（`ChatService` 与 `CustomerSupportService` 仅引用了 `OrganizationService` 中定义的 `TenantContext` 类型，属于类型层面的依赖）。

---

## 四、Store 实现类与 Seeder

本次计划新增了 5 个 `SQLiteStore` 实现类、1 个 `DemoDataSeeder` 和 1 个 CLI 入口。这些类不属于 Application Service（没有业务编排逻辑），而是继承项目既有的 Protocol + 原生 SQLite 模式，直接操作数据库。

### 共计: 5 个 Store 实现类 + 1 个 Seeder + 1 个 CLI + 1 个装配工厂 🆕

---

### 1. `SQLiteCustomerStore` — 客户存储

| 项目 | 内容 |
|---|---|
| **文件** | `app/customers/sqlite_store.py` |
| **实现协议** | `CustomerStore` |
| **依赖** | `upgrade_database(database_path)` — 构造函数中自动执行迁移 |

#### 方法列表 (3 个)

| # | 方法 | 签名 | 作用 | 返回值 |
|---|---|---|---|---|
| 1 | `create_customer` | `(*, organization_id, customer_no, name, email=None, phone=None) -> Customer` | 生成 UUID → INSERT → 按 ID 回读 | `Customer` |
| 2 | `get_by_id` | `(*, organization_id, customer_id) -> Customer` | `WHERE organization_id = ? AND id = ?` | `Customer` |
| 3 | `get_by_no` | `(*, organization_id, customer_no) -> Customer` | `WHERE organization_id = ? AND customer_no = ?` | `Customer` |

---

### 2. `SQLiteOrderStore` — 订单存储

| 项目 | 内容 |
|---|---|
| **文件** | `app/orders/sqlite_store.py` |
| **实现协议** | `OrderStore` |
| **依赖** | `upgrade_database(database_path)` |

#### 方法列表 (3 个)

| # | 方法 | 签名 | 作用 | 返回值 |
|---|---|---|---|---|
| 1 | `create_order` | `(*, organization_id, order_no, customer_id, status, item_summary, total_amount_cents, currency, placed_at, promised_ship_at=None) -> Order` | 生成 UUID → INSERT（含复合 FK 到 customers）→ 按 ID 回读 | `Order` |
| 2 | `get_by_id` | `(*, organization_id, order_id) -> Order` | `WHERE organization_id = ? AND id = ?` | `Order` |
| 3 | `get_by_no` | `(*, organization_id, order_no) -> Order` | `WHERE organization_id = ? AND order_no = ?` | `Order` |

---

### 3. `SQLiteShipmentStore` — 物流存储

| 项目 | 内容 |
|---|---|
| **文件** | `app/shipments/sqlite_store.py` |
| **实现协议** | `ShipmentStore` |
| **依赖** | `upgrade_database(database_path)` |

#### 方法列表 (4 个)

| # | 方法 | 签名 | 作用 | 返回值 |
|---|---|---|---|---|
| 1 | `create_shipment` | `(*, organization_id, shipment_no, order_id, carrier, tracking_no, status, last_event=None, shipped_at=None, estimated_delivery_at=None, delivered_at=None) -> Shipment` | 生成 UUID → INSERT（含复合 FK 到 orders）→ 按 ID 回读 | `Shipment` |
| 2 | `get_by_id` | `(*, organization_id, shipment_id) -> Shipment` | `WHERE organization_id = ? AND id = ?` | `Shipment` |
| 3 | `get_by_no` | `(*, organization_id, shipment_no) -> Shipment` | `WHERE organization_id = ? AND shipment_no = ?` | `Shipment` |
| 4 | `get_by_order_no` | `(*, organization_id, order_no) -> Shipment` | `SELECT s.* FROM shipments AS s JOIN orders AS o ... WHERE s.organization_id = ? AND o.order_no = ?` — 通过 JOIN 按订单号查询物流 | `Shipment` |

---

### 4. `SQLiteTicketStore` — 工单存储 + 评论

| 项目 | 内容 |
|---|---|
| **文件** | `app/tickets/sqlite_store.py` |
| **实现协议** | `TicketStore` |
| **依赖** | `upgrade_database(database_path)` |

#### 工单方法 (3 个)

| # | 方法 | 签名 | 作用 | 返回值 |
|---|---|---|---|---|
| 1 | `create_ticket` | `(*, organization_id, ticket_no, customer_id, order_id=None, created_by_user_id, assigned_to_user_id=None, summary, category, priority, status) -> Ticket` | 生成 UUID → INSERT（含复合 FK 到 customers, orders, memberships）→ 按 ID 回读 | `Ticket` |
| 2 | `get_by_id` | `(*, organization_id, ticket_id) -> Ticket` | `WHERE organization_id = ? AND id = ?` | `Ticket` |
| 3 | `get_by_no` | `(*, organization_id, ticket_no) -> Ticket` | `WHERE organization_id = ? AND ticket_no = ?` | `Ticket` |

#### 评论方法 (2 个)

| # | 方法 | 签名 | 作用 | 返回值 |
|---|---|---|---|---|
| 4 | `add_comment` | `(*, organization_id, ticket_id, author_user_id, visibility, content) -> TicketComment` | 先验证工单存在 → `BEGIN IMMEDIATE` 手动事务 → `SELECT MAX(seq)` 取当前最大序号 → `+1` 作为新序号 → INSERT → 回读 | `TicketComment` |
| 5 | `list_comments` | `(*, organization_id, ticket_id) -> list[TicketComment]` | 先验证工单存在 → `SELECT * FROM ticket_comments WHERE organization_id = ? AND ticket_id = ? ORDER BY seq ASC` | `list[TicketComment]` |

> `add_comment` 使用 `BEGIN IMMEDIATE` 而非依赖 `with connection:` 上下文管理器，是为了确保 `SELECT MAX(seq)` 和 `INSERT` 在同一事务内完成，防止并发场景下 seq 重复。

---

### 5. `DemoDataSeeder` — 模拟数据生成器

| 项目 | 内容 |
|---|---|
| **文件** | `app/demo_data/seeder.py` |
| **依赖** | `OrganizationStore`, `CustomerStore`, `OrderStore`, `ShipmentStore`, `TicketStore`（5 个 Protocol，均通过构造函数注入） |

#### 方法列表 (7 个)

| # | 方法 | 签名 | 作用 | 返回值 |
|---|---|---|---|---|
| 1 | `_require_admin` | `(*, organization_id, actor_user_id) -> None` | 调用 `organization_store.get_membership()` 验证用户角色为 ADMIN，否则抛出 `DemoSeedAdminRequiredError` | `None` |
| 2 | `_ensure_customer` | `(*, organization_id, customer_no, name, email, phone) -> Customer` | 查客户号是否存在 → 不存在则创建 → 存在但数据不一致则抛出 `DemoDataConflictError` | `Customer` |
| 3 | `_ensure_order` | `(*, organization_id, order_no, customer, status, item_summary, total_amount_cents, placed_at, promised_ship_at) -> Order` | 查订单号是否存在 → 不存在则创建 → 存在但字段不一致则抛出 `DemoDataConflictError` | `Order` |
| 4 | `_ensure_shipment` | `(*, organization_id, shipment_no, order, carrier, tracking_no, status, last_event, shipped_at, estimated_delivery_at, delivered_at) -> Shipment` | 按订单号查物流 → 不存在则创建 → 存在但字段不一致则抛出 `DemoDataConflictError` | `Shipment` |
| 5 | `_ensure_ticket` | `(*, organization_id, ticket_no, customer, order, actor_user_id, summary, category, priority, status) -> Ticket` | 查工单号是否存在 → 不存在则创建 → 存在但字段不一致则抛出 `DemoDataConflictError` | `Ticket` |
| 6 | `_ensure_comment` | `(*, organization_id, ticket, actor_user_id, visibility, content) -> None` | 列出现有评论 → 相同内容和可见性已存在则跳过 → 否则调用 `add_comment` | `None` |
| 7 | `seed` | `(*, organization_id, actor_user_id, reference_time=None) -> DemoSeedResult` | 主入口：验证 admin → 生成 2 客户 + 3 订单 + 2 物流 + 2 工单 + 3 评论的三场景固定数据，返回结果摘要。`reference_time` 必须为 timezone-aware datetime | `DemoSeedResult` |

#### 生成的三个固定业务场景

| 场景 | 订单号 | 订单状态 | 物流状态 | 工单 |
|---|---|---|---|---|
| A：延迟未发货 | `ORD-DELAY-001` | `processing` | 无物流记录 | `TKT-DELAY-001`（logistics/high/open） |
| B：运输中 | `ORD-TRANSIT-001` | `shipped` | `in_transit` | 无 |
| C：已签收但破损 | `ORD-DELIVERED-001` | `delivered` | `delivered` | `TKT-DAMAGE-001`（damaged_item/medium/pending_customer + 公开评论要求补充照片） |

---

### 6. `app.demo_data.cli` — 命令行入口

| 项目 | 内容 |
|---|---|
| **文件** | `app/demo_data/cli.py` |
| **入口** | `python -m app.demo_data.cli` |
| **依赖** | 5 个具体 `SQLiteStore` + `get_chat_db_path()` |

#### CLI 参数

| 参数 | 必填 | 说明 |
|---|---|---|
| `--database-path` | 否 | SQLite 数据库路径，默认 `get_chat_db_path()`（环境变量 `CHAT_DB_PATH` 或 `chat_history.db`） |
| `--organization-id` | 是 | 目标企业 ID |
| `--actor-user-id` | 是 | 执行者用户 ID（必须是该企业 admin） |
| `--reference-time` | 否 | ISO 8601 带时区的时间，用于固定 Seeder 中的相对时间 |

#### 函数

| # | 函数 | 签名 | 作用 |
|---|---|---|---|
| 1 | `parse_reference_time` | `(value: str) -> datetime` | 解析 ISO 8601 字符串，验证时区存在 |
| 2 | `build_parser` | `() -> ArgumentParser` | 构建 argparse 解析器 |
| 3 | `main` | `(argv: Sequence[str] \| None = None) -> int` | 组装 Store → 调用 `seeder.seed()` → JSON 输出 → 返回退出码 |

---

### 7. `create_customer_support_service` — 客服服务装配工厂 🆕

| 项目 | 内容 |
|---|---|
| **文件** | `app/application/customer_support_factory.py` |
| **作用** | 一行代码从数据库路径构造完整的 `CustomerSupportService`，调用者无需手动装配四个 Store |

#### 函数

| # | 函数 | 签名 | 作用 | 返回值 |
|---|---|---|---|---|
| 1 | `create_customer_support_service` | `(database_path: str \| Path, *, ticket_no_factory: TicketNumberFactory \| None = None) -> CustomerSupportService` | 用四个 `SQLiteStore` 装配 `CustomerSupportService`，支持可选的自定义工单号工厂 | `CustomerSupportService` |

---

## 五、路由工厂

### 共计: 3 个路由工厂函数，共 9 个 API 端点

所有路由均位于 `app/api/` 目录下，通过 `main.py` 中的 `app.include_router()` 挂载。

---

### 1. `creat_conversation_router` — 会话路由

| 项目 | 内容 |
|---|---|
| **文件** | `app/api/router.py` |
| **URL 前缀** | `/`（无前缀，直接挂载在根路径） |
| **认证方式** | Bearer Token + `X-Organization-ID` 请求头 |

#### 接口列表 (3 个)

| # | 方法 | 路径 | 作用 | 请求体 | 响应模型 |
|---|---|---|---|---|---|
| 1 | `POST` | `/conversations/` | 创建新会话，可指定可选的系统提示词 | `CreateConversationRequest` (`system_prompt: str \| None`) | `ConversationCreated` (201)，含 `conversation_id` |
| 2 | `POST` | `/conversations/{conversation_id}/chat/` | 在已有会话中发送聊天消息，LLM 处理后返回响应（含推理内容和 agent 事件） | `ChatRequest` (`question: str`) | `LLMResponse`，含 `llm_answer`, `llm_reasoning_content`, `events` |
| 3 | `PUT` | `/conversations/{conversation_id}/system-prompt/` | 更新会话的系统提示词 | `UpdateSystemPromptRequest` (`system_prompt: str`) | `SystemPromptUpdated`，含 `updated: bool` |

---

### 2. `create_auth_router` — 认证路由

| 项目 | 内容 |
|---|---|
| **文件** | `app/api/auth_router.py` |
| **URL 前缀** | `/auth` |

#### 接口列表 (3 个)

| # | 方法 | 路径 | 作用 | 认证 | 请求体 | 响应模型 |
|---|---|---|---|---|---|---|
| 1 | `POST` | `/auth/register` | 注册新用户 | 无需认证 | `RegisterRequest` (`username`, `password`) | `UserResponse` (201)，含 `user_id`, `username`, `created_at` |
| 2 | `POST` | `/auth/login` | 用户名密码登录，返回 JWT Bearer Token | 无需认证 | `LoginRequest` (`username`, `password`) | `TokenResponse`，含 `access_token`, `token_type`, `expires_in` |
| 3 | `GET` | `/auth/me` | 获取当前登录用户信息（用于前端刷新页面后验证登录状态） | Bearer Token | 无 | `UserResponse`，含 `user_id`, `username`, `created_at` |

---

### 3. `create_organization_router` — 组织管理路由

| 项目 | 内容 |
|---|---|
| **文件** | `app/api/organization_router.py` |
| **URL 前缀** | `/organizations` |
| **认证方式** | Bearer Token（全部端点需要） |

#### 接口列表 (3 个)

| # | 方法 | 路径 | 作用 | 请求体 | 响应模型 |
|---|---|---|---|---|---|
| 1 | `POST` | `/organizations/` | 创建新组织，创建者自动成为 admin | `CreateOrganizationRequest` (`name: str`) | `OrganizationResponse` (201)，含 `organization_id`, `name` |
| 2 | `GET` | `/organizations/` | 列出当前用户有权访问的所有组织及其角色 | 无 | `list[OrganizationAccessResponse]`，每项含 `organization_id`, `name`, `role` |
| 3 | `POST` | `/organizations/{organization_id}/members/` | 向组织添加成员（仅 admin 可操作） | `AddMemberRequest` (`username`, `role`) | `MembershipResponse` (201)，含 `organization_id`, `user_id`, `role` |

---

### 路由工厂在 `main.py` 中的挂载

```python
# 会话路由 — 根路径
app.include_router(creat_conversation_router(
    chat_service=chat_service,
    get_current_tenant=get_current_tenant,
))

# 认证路由 — 前缀 /auth
app.include_router(create_auth_router(
    auth_service=auth_service,
    get_current_user=get_current_user,
))

# 组织路由 — 前缀 /organizations
app.include_router(create_organization_router(
    organization_service=organization_service,
    get_current_user=get_current_user,
))
```

### 认证依赖链（来自 `app/api/dependencies.py`）

| 依赖工厂 | 作用 |
|---|---|
| `create_current_user_dependency(auth_service)` | 提取 `Authorization: Bearer <token>` → 调用 `auth_service.get_user_from_token()` → 返回 `User`，失败返回 401 |
| `create_current_tenant_dependency(organization_service, get_current_user)` | 先解析当前用户 → 读取 `X-Organization-ID` 请求头 → 调用 `organization_service.get_tenant_context()` → 返回 `TenantContext`，缺失/空时返回 400，无权限返回 404 |

---

### 异常处理的 HTTP 状态码映射

表中记录各端点的异常处理：

| HTTP 状态码 | 异常 | 端点 |
|---|---|---|
| 400 | 缺失 `X-Organization-ID` 头 | 所有需要租户上下文的端点 |
| 401 | `InvalidCredentialsError` | `POST /auth/login` |
| 401 | Token 无效/过期 | `GET /auth/me` 及所有需要认证的端点 |
| 403 | `AdminRoleRequiredError` | `POST /organizations/{id}/members/` |
| 404 | `ConversationNotFoundError` | `POST /conversations/{id}/chat/`, `PUT /conversations/{id}/system-prompt/` |
| 404 | `OrganizationAccessDeniedError` | `POST /organizations/{id}/members/` |
| 404 | `UserNotFoundError` | `POST /organizations/{id}/members/` |
| 409 | `UserAlreadyExistError` | `POST /auth/register` |
| 409 | `MembershipAlreadyExistsError` | `POST /organizations/{id}/members/` |
| 422 | `InvalidUsernameError`, `PasswordPolicyError` | `POST /auth/register` |
| 422 | `InvalidOrganizationNameError` | `POST /organizations/` |
| 422 | 空 `conversation_id` 或空 `question` 或空 `system_prompt` | 会话相关端点 |

---

## 附录：数据库表完整字段一览

所有 11 张数据库表的完整 SQL 字段（综合 Alembic 迁移脚本，新增 5 张业务表用 🆕 标记）:

### 表: `users`

| 列名 | 类型 | 约束 |
|---|---|---|
| `id` | TEXT | PRIMARY KEY |
| `username` | TEXT | UNIQUE, COLLATE NOCASE |
| `password_hash` | TEXT | NOT NULL |
| `created_at` | TEXT | NOT NULL, DEFAULT CURRENT_TIMESTAMP |

### 表: `conversations`

| 列名 | 类型 | 约束 |
|---|---|---|
| `id` | TEXT | PRIMARY KEY |
| `organization_id` | TEXT | NOT NULL, 复合 FK → `memberships(organization_id, user_id)` |
| `user_id` | TEXT | NOT NULL |
| `system_prompt` | TEXT | 可空 |
| `created_at` | TEXT | NOT NULL, DEFAULT CURRENT_TIMESTAMP |
| `updated_at` | TEXT | NOT NULL, DEFAULT CURRENT_TIMESTAMP |

### 表: `messages`

| 列名 | 类型 | 约束 |
|---|---|---|
| `id` | INTEGER | PRIMARY KEY AUTOINCREMENT |
| `conversation_id` | TEXT | NOT NULL, FK → `conversations.id` |
| `seq` | INTEGER | NOT NULL, UNIQUE(conversation_id, seq) |
| `role` | TEXT | NOT NULL |
| `payload_json` | TEXT | NOT NULL |
| `created_at` | TEXT | NOT NULL, DEFAULT CURRENT_TIMESTAMP |

### 表: `organizations`

| 列名 | 类型 | 约束 |
|---|---|---|
| `id` | TEXT | PRIMARY KEY |
| `name` | TEXT | NOT NULL |
| `created_at` | TEXT | NOT NULL, DEFAULT CURRENT_TIMESTAMP |

### 表: `memberships`

| 列名 | 类型 | 约束 |
|---|---|---|
| `organization_id` | TEXT | NOT NULL, FK → `organizations.id`, 复合 PK |
| `user_id` | TEXT | NOT NULL, FK → `users.id`, 复合 PK |
| `role` | TEXT | NOT NULL, CHECK(role IN ('admin','agent')) |
| `created_at` | TEXT | NOT NULL, DEFAULT CURRENT_TIMESTAMP |

### 🆕 表: `customers`

| 列名 | 类型 | 约束 |
|---|---|---|
| `id` | TEXT | PRIMARY KEY |
| `organization_id` | TEXT | NOT NULL, FK → `organizations.id` |
| `customer_no` | TEXT | NOT NULL, UNIQUE(organization_id, customer_no), CHECK(length(trim(customer_no)) > 0) |
| `name` | TEXT | NOT NULL, CHECK(length(trim(name)) > 0) |
| `email` | TEXT | 可空 |
| `phone` | TEXT | 可空 |
| `created_at` | TEXT | NOT NULL, DEFAULT CURRENT_TIMESTAMP |

### 🆕 表: `orders`

| 列名 | 类型 | 约束 |
|---|---|---|
| `id` | TEXT | PRIMARY KEY |
| `organization_id` | TEXT | NOT NULL, 复合 FK → `customers(organization_id, id)` |
| `order_no` | TEXT | NOT NULL, UNIQUE(organization_id, order_no), CHECK(length(trim(order_no)) > 0) |
| `customer_id` | TEXT | NOT NULL |
| `status` | TEXT | NOT NULL, CHECK(status IN ('pending_payment','paid','processing','shipped','delivered','cancelled','refunded')) |
| `item_summary` | TEXT | NOT NULL, CHECK(length(trim(item_summary)) > 0) |
| `total_amount_cents` | INTEGER | NOT NULL, CHECK(total_amount_cents >= 0) |
| `currency` | TEXT | NOT NULL, CHECK(length(currency) = 3 AND currency = upper(currency)) |
| `placed_at` | TEXT | NOT NULL |
| `promised_ship_at` | TEXT | 可空 |
| `created_at` | TEXT | NOT NULL, DEFAULT CURRENT_TIMESTAMP |
| `updated_at` | TEXT | NOT NULL, DEFAULT CURRENT_TIMESTAMP |

### 🆕 表: `shipments`

| 列名 | 类型 | 约束 |
|---|---|---|
| `id` | TEXT | PRIMARY KEY |
| `organization_id` | TEXT | NOT NULL, 复合 FK → `orders(organization_id, id)` |
| `shipment_no` | TEXT | NOT NULL, UNIQUE(organization_id, shipment_no), CHECK(length(trim(shipment_no)) > 0) |
| `order_id` | TEXT | NOT NULL, UNIQUE(organization_id, order_id) — 一个订单最多一条物流 |
| `carrier` | TEXT | NOT NULL, CHECK(length(trim(carrier)) > 0) |
| `tracking_no` | TEXT | NOT NULL, UNIQUE(organization_id, tracking_no), CHECK(length(trim(tracking_no)) > 0) |
| `status` | TEXT | NOT NULL, CHECK(status IN ('pending','picked_up','in_transit','delivered','exception','returned')) |
| `last_event` | TEXT | 可空 |
| `shipped_at` | TEXT | 可空 |
| `estimated_delivery_at` | TEXT | 可空 |
| `delivered_at` | TEXT | 可空 |
| `created_at` | TEXT | NOT NULL, DEFAULT CURRENT_TIMESTAMP |
| `updated_at` | TEXT | NOT NULL, DEFAULT CURRENT_TIMESTAMP |

### 🆕 表: `tickets`

| 列名 | 类型 | 约束 |
|---|---|---|
| `id` | TEXT | PRIMARY KEY |
| `organization_id` | TEXT | NOT NULL |
| `ticket_no` | TEXT | NOT NULL, UNIQUE(organization_id, ticket_no), CHECK(length(trim(ticket_no)) > 0) |
| `customer_id` | TEXT | NOT NULL, 复合 FK → `customers` |
| `order_id` | TEXT | 可空，复合 FK → `orders(organization_id, id, customer_id)` |
| `created_by_user_id` | TEXT | NOT NULL, 复合 FK → `memberships(organization_id, user_id)` |
| `assigned_to_user_id` | TEXT | 可空，复合 FK → `memberships(organization_id, user_id)` |
| `summary` | TEXT | NOT NULL, CHECK(length(trim(summary)) > 0) |
| `category` | TEXT | NOT NULL, CHECK(category IN ('logistics','refund','damaged_item','product_question','other')) |
| `priority` | TEXT | NOT NULL, CHECK(priority IN ('low','medium','high','urgent')) |
| `status` | TEXT | NOT NULL, CHECK(status IN ('open','in_progress','pending_customer','resolved','closed')) |
| `created_at` | TEXT | NOT NULL, DEFAULT CURRENT_TIMESTAMP |
| `updated_at` | TEXT | NOT NULL, DEFAULT CURRENT_TIMESTAMP |

### 🆕 表: `ticket_comments`

| 列名 | 类型 | 约束 |
|---|---|---|
| `id` | TEXT | PRIMARY KEY |
| `organization_id` | TEXT | NOT NULL, 复合 FK → `tickets(organization_id, id)` |
| `ticket_id` | TEXT | NOT NULL |
| `seq` | INTEGER | NOT NULL, UNIQUE(organization_id, ticket_id, seq), CHECK(seq > 0) |
| `author_user_id` | TEXT | NOT NULL, 复合 FK → `memberships(organization_id, user_id)` |
| `visibility` | TEXT | NOT NULL, CHECK(visibility IN ('internal','public')) |
| `content` | TEXT | NOT NULL, CHECK(length(trim(content)) > 0) |
| `created_at` | TEXT | NOT NULL |

---

*本文档由 Claude Code 自动分析生成，统计截止日期: 2026-07-30*
